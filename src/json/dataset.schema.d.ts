/* eslint-disable */
/**
* This file was automatically generated by json-schema-to-typescript.
* DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
* and run json-schema-to-typescript to regenerate this file.
*/

/**
 * Transliteration of 'Kadmos' using the dataset's script
 */
export type SchemaGameHeadingConfig = ({
type?: "string"
string: string
font?: SchemaFontReference
} | {
type: "custom"
key: string
data?: unknown[]
})
export type FontWeight = number
export type SchemaLetterConfig = (SchemaStringLetterConfig | SchemaSVGLetterConfig | SchemaBrailleLetterConfig)
export type SchemaQuizAnswerConfig = ({
type: "string"
properties?: SchemaStringAnswerConfig
} | {
type: ("number" | "integer")
properties?: {
/**
 * Maximum distance from value to pass.
 */
maxDist?: number
/**
 * "linear" (default), "log" or "log{BASE}" where BASE is the log base to use, default base is 10.
 */
distanceMode?: string
}
} | SchemaListAnswerConfig)
export type SchemaSelectorBlock = ((SchemaSelectorGridBlock | {
style?: SchemaSelectorBlockStyle
grid?: false
}) & {
letters?: SchemaLetterRanges
})
/**
 * If string, split up into characters as labels. Use null for a gap instead of a label.
 */
export type BlockLabels = (string | null)[]
/**
 * How elements are selected using range selection.
 */
export type SchemaGridRangeMode = ("grid" | "walkRows" | "walkColumns")
/**
 * Comma-separated list of 'rectangle' ranges (ROWS|COLUMNS), where ROWS and COLUMNS are in range-syntax.
 */
export type MatrixRanges = string
/**
 * Inclusive ranges denoted by `start:stop`, separated by commas.
 * 
 * This interface was referenced by `SchemaLetterRanges`'s JSON-Schema definition
 * via the `patternProperty` "^(form(:[a-z]+)?|prop:[a-z]+(>(v|var|variant):[a-z]+)?(>(l|lang|language):[a-z]+)?)$".
 */
export type LetterRange = string
export type SchemaItemsData = [unknown[], string[]][]
export type SchemaCombineTemplates = [(string | null)[], string][]
export type RegExpFlags = string

export interface SchemaKadmosDataset {
metadata: SchemaMetadata
letterConfig: SchemaLetterConfig
languages?: SchemaLanguages
variants?: SchemaVariantsConfig
game?: SchemaGameConfig
subsets: SchemaSubsets
combine?: SchemaCombineConfig
selector?: SchemaGlobalSelectorConfig
}
export interface SchemaMetadata {
/**
 * Unique key for this script dataset. Preferably from the <a href="https://www.iana.org/assignments/language-subtag-registry/language-subtag-registry">IANA Language Subtag Registry</a>.
 */
key: string
/**
 * Name of the dataset
 */
name: string
description?: string
/**
 * cf. https://en.wikipedia.org/wiki/IETF_language_tag
 */
lang?: string
/**
 * Direction of the script: ltr for left-to-right (default) or rtl for right-to-left.
 */
dir?: ("ltr" | "rtl")
gameHeading?: SchemaGameHeadingConfig
terms?: SchemaTermsConfig
}
export interface SchemaFontReference {
/**
 * One of the keys set in the letterConfig.fonts object.
 */
key?: string
params?: SchemaFontParams
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaFontParams {
weight?: FontWeight
/**
 * Scale the font-size by this factor.
 */
scale?: number
/**
 * Font up/down shift in em units. Negative: up, positive: down.
 */
shift?: number
/**
 * CSS property font-variant-alternates: styleset(...).
 */
styleset?: string
/**
 * CSS letter-spacing property.
 */
letterSpacing?: string
/**
 * CSS line-height property.
 */
lineHeight?: number
}
/**
 * Specify what the equivalent of e.g. "letter" and "letters" in this font are called.
 */
export interface SchemaTermsConfig {
letter?: string
/**
 * Uses the term for letter + 's' by default.
 */
letters?: string
}
export interface SchemaStringLetterConfig {
type: "string"
fonts: SchemaFonts
}
/**
 * A registry of the fonts used by the dataset, with lowercase keys.
 */
export interface SchemaFonts {
data: {
[k: string]: SchemaFontData
}
params?: SchemaFontParams1
/**
 * List of unicode characters manually to be included.
 */
manualUnicodes?: string
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaFontData {
/**
 * Matches the font family by default.
 */
label?: string
/**
 * Font family name. E.g. "Noto Sans" or "Noto Serif Hebrew".
 */
family: string
default?: boolean
params?: SchemaFontParams
}
/**
 * Params applied to all fonts.
 */
export interface SchemaFontParams1 {
weight?: FontWeight
/**
 * Scale the font-size by this factor.
 */
scale?: number
/**
 * Font up/down shift in em units. Negative: up, positive: down.
 */
shift?: number
/**
 * CSS property font-variant-alternates: styleset(...).
 */
styleset?: string
/**
 * CSS letter-spacing property.
 */
letterSpacing?: string
/**
 * CSS line-height property.
 */
lineHeight?: number
}
export interface SchemaSVGLetterConfig {
type: "svg"
template: SchemaSVGTemplate
}
export interface SchemaSVGTemplate {
/**
 * Template string with $key$ for key substitutions
 */
string: string
/**
 * Keys in order used for the substitutions in the svg template.
 */
keys: string[]
}
export interface SchemaBrailleLetterConfig {
type: "braille"
}
/**
 * List of languages that you can play the game in.
 */
export interface SchemaLanguages {
/**
 * Language subtags. In items, use "p:PROPERTY_SUBTAG" (e.g. "p:name_de") as key to specify language-specific property values.
 */
keys: string[]
default?: string
}
export interface SchemaVariantsConfig {
data: {
[k: string]: SchemaVariantData
}
groups?: {
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
[k: string]: {
label: string
/**
 * List of keys from the variants.data field.
 */
keys: string[]
}
}
setting?: {
type?: ("select" | "buttonGroup")
label?: string
}
/**
 * If true, the lang attribute of a variant will be set to its key unless already specified.
 */
useKeyAsLang?: boolean
default?: string
}
/**
 * A variant of the script, e.g. the Russian or Bulgarian variant of the Cyrillic script.
 * 
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaVariantData {
label: string
/**
 * Modifying the HTML lang attribute of the symbols. By default, the key pointing to this entry is used.
 */
lang?: string
default?: boolean
/**
 * Modifying the dataset's fonts entries.
 */
fontParams?: {
[k: string]: SchemaFontParams
}
}
export interface SchemaGameConfig {
defaultWeight?: FontWeight
}
export interface SchemaSubsets {
[k: string]: SchemaSubset
}
/**
 * This interface was referenced by `SchemaSubsets`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaSubset {
label: string
properties: SchemaSubsetProperties
selector: SchemaSelectorConfig
forms?: SchemaFormsConfig
items: SchemaSubsetItems
variants?: SchemaSubsetVariants
}
export interface SchemaSubsetProperties {
[k: string]: SchemaSubsetPropertyParams
}
/**
 * This interface was referenced by `SchemaSubsetProperties`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaSubsetPropertyParams {
label: string
config: SchemaQuizAnswerConfig
active?: boolean
}
export interface SchemaStringAnswerConfig {
/**
 * Maximum distance (optimal string alignment) to pass.
 */
maxDist?: number
/**
 * If value is n, add 1 to maxDist for every n characters of the word.
 */
maxDistMult?: number
/**
 * Search-replace entries, using regular expressions, to describe equivalent substitutions that the answerer can use.
 */
substitutions?: {
pattern: string
repl: string[]
}[]
/**
 * If true, the substitutions need to be used if applicable.
 */
forceSubstitutions?: boolean
/**
 * Whether the answer is case-sensitive.
 */
caseSensitive?: boolean
/**
 * Used to ignore content in brackets or parentheses for grading. String of opening bracket characters, e.g. "([" to ignore (...) and [...].
 */
ignoreBrackets?: string
}
export interface SchemaListAnswerConfig {
type: "list"
properties?: {
/**
 * RegExp to split input into list of answers.
 */
splitter?: string
/**
 * Whether one or all of the entries need to be matched. One will use the best score of the entries, All will use the average.
 */
gradeMode?: ("one" | "all")
}
items: SchemaQuizAnswerConfig
}
export interface SchemaSelectorConfig {
blocks?: SchemaSelectorBlock[]
font?: SchemaFontReference
/**
 * Settings for the label underneath letters in the selector. If not set, these won't appear.
 */
label?: {
/**
 * Which property to label the items with.
 */
property: string
/**
 * If true, only shows the first value of a list property.
 */
splitFirst?: boolean
}
style?: SchemaSelectorBlockStyle
/**
 * 'all', 'none' or 'block{INDEX}'.
 */
defaultActive?: string
}
export interface SchemaSelectorGridBlock {
style?: SchemaSelectorBlockStyle
grid: true
dimensions: [number, number]
columnLabels?: BlockLabels
rowLabels?: BlockLabels
columnLabelPosition?: ("start" | "end" | "both")
rowLabelPosition?: ("start" | "end" | "both")
columnLabelSpans?: number[]
rowLabelSpans?: number[]
rangeMode?: SchemaGridRangeMode
/**
 * How to fill the grid with items from the array.
 */
fillDirection?: ("rows" | "columns")
gaps?: MatrixRanges
}
export interface SchemaSelectorBlockStyle {
/**
 * Equivalent to setting buttonMinWidth and buttonMaxWidth to the same value.
 */
buttonWidth?: (string | number)
buttonMinWidth?: (string | number)
buttonMaxWidth?: (string | number)
symbolSize?: number
symbolMinWidth?: string
labelGap?: number
}
/**
 * This interface was referenced by `SchemaVariantLetterRanges`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaLetterRanges {
/**
 * 0-based index ranges.
 */
i0?: string
/**
 * 1-based index ranges.
 */
i1?: string
[k: string]: LetterRange | undefined
}
export interface SchemaFormsConfig {
/**
 * A form of letter, e.g. lower- or uppercase.
 */
data: {
[k: string]: SchemaFormConfig
}
/**
 * Label of the forms setting. Default "Forms".
 */
label?: string
/**
 * If true, you can only select one form to play at a time.
 */
exclusive?: boolean
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaFormConfig {
combine?: string[]
label: string
active?: boolean
/**
 * If set, this form will not appear in the settings, but will follow the setting of the given key.
 */
groupWith?: string
}
export interface SchemaSubsetItems {
/**
 * Keys to assign the property entries of data rows to. RegExp-ish pattern: `PROPERTY(>(v|var|variant):VARIANT)?(>(l|lang|language):LANGUAGE)?`
 */
properties: string[]
/**
 * Array with [[...forms], [...properties]] entries matching the template. If first element is a string, it will be split into individual characters for forms. Use a single-item array if this isn't wanted.
 */
data: SchemaItemsData
}
export interface SchemaSubsetVariants {
letters: SchemaVariantLetterRanges
}
/**
 * Set the letter ranges for the variants defined globally.
 */
export interface SchemaVariantLetterRanges {
[k: string]: SchemaLetterRanges
}
export interface SchemaCombineConfig {
methods: {
[k: string]: SchemaCombineMethod
}
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaCombineMethod {
label: string
subsets: string[]
letterConfig?: (null | {
form: string
})[]
properties?: {
[k: string]: SchemaCombinePropertyConfig
}
templates?: SchemaCombineTemplates
regExpFlags?: RegExpFlags
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^[a-z]+$".
 */
export interface SchemaCombinePropertyConfig {
sources: (string | string[])
templates?: SchemaCombineTemplates
regExpFlags?: RegExpFlags
}
export interface SchemaGlobalSelectorConfig {
style?: SchemaSelectorBlockStyle
}
